# 🪟 Windows Setup - Visual Guide

## 🎯 The Complete Picture

```
┌──────────────────────────────────────────────────────────┐
│           YOUR WINDOWS COMPUTER                          │
│                                                          │
│  ┌────────────────────────────────────────────────┐    │
│  │  PREREQUISITES (Install Once)                  │    │
│  │  ─────────────────────────────────             │    │
│  │  ✅ Python 3.8+                                │    │
│  │  ✅ Node.js 18+                                │    │
│  │  ✅ MongoDB                                    │    │
│  └────────────────────────────────────────────────┘    │
│                      ↓                                   │
│  ┌────────────────────────────────────────────────┐    │
│  │  DOUBLE-CLICK: START_WINDOWS.bat               │    │
│  └────────────────────────────────────────────────┘    │
│                      ↓                                   │
│  ┌──────────────┬──────────────┬──────────────┐        │
│  │   MongoDB    │   Backend    │  Frontend    │        │
│  │   Starts     │   Starts     │   Starts     │        │
│  │   (Auto)     │  (Port 8001) │ (Port 3000)  │        │
│  └──────────────┴──────────────┴──────────────┘        │
│                      ↓                                   │
│  ┌────────────────────────────────────────────────┐    │
│  │  BROWSER OPENS AUTOMATICALLY                   │    │
│  │  http://localhost:3000                         │    │
│  │                                                │    │
│  │  ╔══════════════════════════════════════╗     │    │
│  │  ║  StockBreak Pro Dashboard            ║     │    │
│  │  ║  ─────────────────────────────       ║     │    │
│  │  ║  📊 Stocks Scanned: 20               ║     │    │
│  │  ║  📈 Breakouts Found: 8               ║     │    │
│  │  ║                                      ║     │    │
│  │  ║  🔍 Search: _____________            ║     │    │
│  │  ║                                      ║     │    │
│  │  ║  [Stock Card 1] [Stock Card 2]      ║     │    │
│  │  ║  [Stock Card 3] [Stock Card 4]      ║     │    │
│  │  ╚══════════════════════════════════════╝     │    │
│  └────────────────────────────────────────────────┘    │
│                                                          │
│  ✨ ENJOY YOUR STOCK ANALYSIS APP! ✨                   │
└──────────────────────────────────────────────────────────┘
```

---

## 📂 File Structure Visual

```
C:\StockBreakPro\
│
├── 📄 START_WINDOWS.bat          ← DOUBLE-CLICK THIS!
├── 📄 STOP_WINDOWS.bat           ← Use to stop
├── 📄 WINDOWS_QUICK_START.md     ← Quick guide
├── 📄 WINDOWS_SETUP_GUIDE.md     ← Detailed guide
│
├── 📁 backend\
│   ├── server.py                 ← Backend code
│   ├── requirements.txt          ← Python packages
│   ├── .env                      ← Backend config
│   └── venv\                     ← Virtual environment
│
├── 📁 frontend\
│   ├── src\                      ← React source
│   ├── public\                   ← Static files
│   ├── package.json              ← Node packages
│   └── .env                      ← Frontend config
│
└── 📁 mobile\                     ← Android app (separate)
```

---

## 🔄 Application Flow

```
┌─────────────────────────────────────────────────────────────┐
│  STEP 1: START_WINDOWS.bat                                  │
└─────────────────────────────────────────────────────────────┘
                              ↓
        ┌────────────────────────────────────────┐
        │  Check Python, Node.js, MongoDB        │
        └────────────────────────────────────────┘
                              ↓
        ┌────────────────────────────────────────┐
        │  Start MongoDB Service                 │
        └────────────────────────────────────────┘
                              ↓
        ┌────────────────────────────────────────┐
        │  Start Backend (Port 8001)             │
        │  • Create venv if needed               │
        │  • Install dependencies                │
        │  • Run: uvicorn server:app             │
        └────────────────────────────────────────┘
                              ↓
        ┌────────────────────────────────────────┐
        │  Start Frontend (Port 3000)            │
        │  • Install dependencies if needed      │
        │  • Run: npm start                      │
        └────────────────────────────────────────┘
                              ↓
        ┌────────────────────────────────────────┐
        │  Open Browser                          │
        │  http://localhost:3000                 │
        └────────────────────────────────────────┘
                              ↓
        ┌────────────────────────────────────────┐
        │  ✅ APPLICATION RUNNING!                │
        └────────────────────────────────────────┘
```

---

## 🌐 Network Access Diagram

```
                YOUR COMPUTER
                ┌─────────────────────┐
                │  Backend: 8001      │
                │  Frontend: 3000     │
                │  IP: 192.168.1.100  │
                └─────────────────────┘
                          │
                    WiFi Router
                          │
        ┌─────────────────┼─────────────────┐
        │                 │                 │
   Your Phone        Your Tablet      Other Computer
   (Access via)      (Access via)     (Access via)
   192.168.1.100:3000
```

---

## 🖱️ What Happens When You Click START

```
[You] Double-click START_WINDOWS.bat
   ↓
[Script] Checks if Python installed
   ↓
   ✅ Found → Continue
   ❌ Not Found → Show error, exit
   ↓
[Script] Checks if Node.js installed
   ↓
   ✅ Found → Continue
   ❌ Not Found → Show error, exit
   ↓
[Script] Starts MongoDB
   ↓
   ✅ Started → Continue
   ⚠️  Already running → Continue anyway
   ↓
[Script] Opens new window: Backend
   ↓
[Backend Window] Creates venv (if needed)
[Backend Window] Installs requirements (if needed)
[Backend Window] Runs uvicorn server:app
   ↓
   ✅ Backend running on http://0.0.0.0:8001
   ↓
[Script] Opens new window: Frontend
   ↓
[Frontend Window] Installs node_modules (if needed)
[Frontend Window] Runs npm start
   ↓
   ✅ Frontend compiled successfully
   ✅ Running on http://localhost:3000
   ↓
[Script] Waits 15 seconds
[Script] Opens default browser
   ↓
[Browser] Loads http://localhost:3000
[Browser] Shows StockBreak Pro Dashboard
   ↓
   🎉 SUCCESS! You can now use the app!
```

---

## 🎯 Port Usage

```
┌────────────────────────────────────────┐
│  YOUR COMPUTER                         │
│                                        │
│  Port 27017  ← MongoDB                 │
│  Port 8001   ← Backend API             │
│  Port 3000   ← Frontend Website        │
└────────────────────────────────────────┘
```

### How They Communicate:

```
Browser (You)
    ↓ Request page
Frontend (Port 3000)
    ↓ Request data
Backend (Port 8001)
    ↓ Query data
MongoDB (Port 27017)
    ↓ Return data
Backend
    ↓ Return JSON
Frontend
    ↓ Display UI
Browser (You see it!)
```

---

## 📊 System Resources

```
┌─────────────────────────────────────────────┐
│  TYPICAL RESOURCE USAGE                     │
├─────────────────────────────────────────────┤
│  MongoDB:     ~200 MB RAM                   │
│  Backend:     ~150 MB RAM                   │
│  Frontend:    ~300 MB RAM                   │
│  ─────────────────────────────────          │
│  Total:       ~650 MB RAM                   │
│  Disk:        ~500 MB                       │
│  CPU:         5-10% (idle)                  │
└─────────────────────────────────────────────┘
```

---

## ⏱️ Timing Breakdown

```
First Time Setup:
├── Install Python:        5 minutes
├── Install Node.js:       3 minutes
├── Install MongoDB:       5 minutes
├── Run START_WINDOWS:     2 minutes
│   ├── Create venv:       30 seconds
│   ├── Install pip deps:  1 minute
│   ├── Install npm deps:  2 minutes
│   └── Start services:    30 seconds
└── Total:                ~15-20 minutes

Subsequent Starts:
├── Run START_WINDOWS:     20 seconds
└── Ready to use!
```

---

## 🔐 Firewall Configuration

```
Windows Firewall
    ↓
Allow Inbound Rules:
    ├── Port 3000 (TCP)  ← Frontend
    ├── Port 8001 (TCP)  ← Backend
    └── Port 27017 (TCP) ← MongoDB
```

To allow network access:
1. Windows Security → Firewall
2. Advanced Settings → Inbound Rules
3. New Rule → Port → TCP → 3000, 8001
4. Allow connection → Name: StockBreak Pro

---

## 🎨 UI Components

```
StockBreak Pro Dashboard
┌─────────────────────────────────────────────────┐
│  Header                                          │
│  ├── Logo: StockBreak Pro                       │
│  ├── Search Bar                                  │
│  └── Settings Icon                               │
├─────────────────────────────────────────────────┤
│  Statistics Cards                                │
│  ┌──────────┐  ┌──────────┐                    │
│  │ Scanned  │  │Breakouts │                    │
│  │   20     │  │    8     │                    │
│  └──────────┘  └──────────┘                    │
├─────────────────────────────────────────────────┤
│  Stock Cards (Grid)                              │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐          │
│  │ RELIANCE│ │   TCS   │ │  INFY   │          │
│  │ ₹2,450  │ │ ₹3,680  │ │ ₹1,520  │          │
│  │ +2.3%   │ │ +1.8%   │ │ +0.9%   │          │
│  │ BUY     │ │ WAIT    │ │ BUY     │          │
│  └─────────┘ └─────────┘ └─────────┘          │
│                                                  │
│  (More stocks...)                                │
└─────────────────────────────────────────────────┘
```

---

## ✅ Visual Success Checklist

```
Prerequisites:
[ ] Python installed ───────→ ✓ python --version works
[ ] Node.js installed ──────→ ✓ node --version works
[ ] MongoDB installed ──────→ ✓ mongod --version works

Startup:
[ ] START_WINDOWS.bat runs ─→ ✓ No errors shown
[ ] Backend window opens ───→ ✓ Shows "Uvicorn running"
[ ] Frontend window opens ──→ ✓ Shows "Compiled successfully"
[ ] Browser opens ──────────→ ✓ Goes to localhost:3000

Application:
[ ] Dashboard loads ────────→ ✓ Purple theme visible
[ ] Stock cards visible ────→ ✓ Shows stock data
[ ] Search works ───────────→ ✓ Can filter stocks
[ ] Click stock works ──────→ ✓ Opens detail page
[ ] Watchlist works ────────→ ✓ Can add/remove stocks

🎊 ALL CHECKS PASSED = SUCCESS!
```

---

## 🆘 Quick Error Reference

```
ERROR                           FIX
────────────────────────────────────────────────────
Python not found                Install Python
Node.js not found              Install Node.js
Port already in use            Kill process: taskkill
MongoDB not running            net start MongoDB
Cannot find server.py          Run from correct folder
Module not found              pip install -r requirements.txt
Dependencies error            npm install in frontend/
Blank page                    Check backend is running
Connection refused            Check firewall settings
```

---

**Everything you need is visual and clear! Follow START_WINDOWS.bat and enjoy!** 🎉
